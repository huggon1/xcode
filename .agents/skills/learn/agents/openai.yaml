interface:
  display_name: "Learn"
  short_description: "Capture a learned skill or a human improvement item"
  default_prompt: "Capture explicit learning from recent work. If no instruction is given, enter guided learning triage and explain the likely outputs. If an in-scope instruction is given, bias toward learned skill, human-improvement item, both, or evaluation, then preview the result and write only after confirmation."
policy:
  allow_implicit_invocation: false
